import { Award } from 'lucide-react';
import DocumentPage from './DocumentPage';

export default function Certificates() {
  return (
    <DocumentPage documentType="certificate" title="Certificates" icon={Award} />
  );
}
