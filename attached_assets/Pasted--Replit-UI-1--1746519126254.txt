Ты — фронтенд-разработчик, работающий в Replit. Улучши UI таблицы «Учебный план»:

1. **Фиксированные размер и стили колонок**  
   - Колонка «Дисциплины»: `min-w-[200px]` и `sticky left-0 bg-slate-900 text-white z-10`.  
   - «Форма контроля» / «Итого акад. часов» / «Объем ОП»: `w-[100px]` и `text-center`.  
   - Каждая семестровая колонка: `w-[60px]` и `text-center`.  

2. **Заголовок в несколько уровней**  
   - Используй `<thead className="sticky top-0 z-20">`.  
   - Реализуй три строки заголовка с `rowSpan`/`colSpan`:  
     1) Курсы: `<th colSpan={numSemestersPerCourse} className="bg-slate-800 text-white">Курс 1</th>`  
     2) Семестры: `<th colSpan={1} className="bg-slate-700 text-white">Семестр 1 (18 нед.)</th>`  
     3) Тип часов: каждый `<th className="bg-slate-600 text-xs uppercase whitespace-nowrap">Лек</th>`, `<th>Лаб</th>`, `<th>ПР</th>`, `<th>КР</th>`, `<th>Итог</th>`.  
   - В третьем уровне сделай текст мелким (`text-[10px] leading-tight`) и поверни его вертикально:  
     ```css
     .rotate-header { transform: rotate(-90deg); transform-origin: bottom left; }
     ```  

3. **Ограничение текста и выравнивание**  
   - Во всех `th, td`: `@apply px-2 py-1 whitespace-nowrap overflow-hidden text-ellipsis`.  
   - Заголовки обрезаются с троеточием, но всегда читаются при наведении (tooltip).

4. **Итого-строка**  
   - Добавь `<tfoot className="sticky bottom-0 bg-slate-900 text-white">…</tfoot>` с суммами и `text-center`.

5. **Стилизация строк**  
   - Разделы/группы: `bg-slate-800` и `font-semibold`.  
   - Обычные предметы: чередование `bg-slate-900` / `bg-slate-950`.  
   - При наведении `tr:hover { @apply bg-slate-700 }`.

6. **Редактирование ячеек**  
   - По двойному клику превращай `td` в `<input type="number" className="w-full text-right bg-transparent p-1" />`.  
   - При фокусе на input добавляй обводку: `focus:ring-1 focus:ring-primary`.

7. **Scrollbar**  
   - В контейнере `overflow-x-auto`:  
     ```css
     &::-webkit-scrollbar { height: 6px; }
     &::-webkit-scrollbar-thumb { @apply bg-slate-600 rounded-full; }
     ```

Проверь, чтобы при ширине экрана 1280px:
- Левый столбец и шапка оставались на месте при скролле.  
- Тексты внутри ячеек не наслаивались и не вылезали.  
- «Итого» всегда была видна.  
- Заголовки третьего уровня читаемы (поворот и мелкий шрифт).  